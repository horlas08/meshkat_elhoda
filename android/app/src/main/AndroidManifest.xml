<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.meshkatelhoda.pro">
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    
    <!-- الأذونات المطلوبة -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    
    <!-- Exact Alarm Permissions -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- USE_EXACT_ALARM for Android 14+ (API 34+) - doesn't require runtime permission -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    
    <!-- Foreground Service Permissions for Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Lock screen and wake permissions -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    
    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <uses-permission android:name="com.android.vending.BILLING" />
    
    <application
        android:label="Mishkat Al-Hoda Pro"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/launcher_icon">

        <activity
            android:name=".AthanFlutterOverlayActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/AthanAlarmTheme" />

        <activity
            android:name=".AthanAlarmActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/AthanAlarmTheme" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4204070072716559~8520290982" 
        />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDmgxQIRDwTzrU6ph702YXadBPLSzv2pnc"
        />

         <!-- أضف هذا الـ Receiver -->
    <receiver 
        android:name=".BootCompletedReceiver"
        android:enabled="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED"/>
            <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
        </intent-filter>
    </receiver>

    <!-- Athan BroadcastReceiver - for playing Athan when app is terminated -->
    <receiver
        android:name=".AthanBroadcastReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.PLAY_ATHAN"/>
        </intent-filter>
    </receiver>

    <!-- Athan Foreground Service - for reliable Athan playback on Android 14+ -->
    <service
        android:name=".AthanForegroundService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback"
        tools:ignore="Instantiatable">
    </service>

    <!-- Athan Stop Receiver - for stopping Athan from notification -->
    <receiver
        android:name=".AthanStopReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.STOP_ATHAN"/>
        </intent-filter>
    </receiver>

    <!-- Pre-Athan Reminder Receiver - shows reminder notification (e.g., 5 minutes before prayer) -->
    <receiver
        android:name=".PreAthanReminderReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.PRE_ATHAN_REMINDER"/>
        </intent-filter>
    </receiver>

    <!-- Smart Dhikr BroadcastReceiver - for playing Smart Voice when app is terminated -->
    <receiver
        android:name=".SmartDhikrBroadcastReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.PLAY_SMART_DHIKR"/>
        </intent-filter>
    </receiver>

    <!-- Ramadan (Suhoor/Iftar) BroadcastReceiver - for playing reminders when app is terminated -->
    <receiver
        android:name=".RamadanReminderReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.PLAY_RAMADAN_REMINDER"/>
        </intent-filter>
    </receiver>

    <!-- Smart Dhikr Foreground Service - for reliable Smart Voice playback -->
    <service
        android:name=".SmartDhikrForegroundService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback"
        tools:ignore="Instantiatable">
    </service>

    <!-- Ramadan Foreground Service - for reliable Suhoor/Iftar playback -->
    <service
        android:name=".RamadanReminderForegroundService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="mediaPlayback"
        tools:ignore="Instantiatable">
    </service>

    <!-- Smart Dhikr Stop Receiver -->
    <receiver
        android:name=".SmartDhikrStopReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.STOP_SMART_DHIKR"/>
        </intent-filter>
    </receiver>

    <!-- Ramadan Stop Receiver -->
    <receiver
        android:name=".RamadanReminderStopReceiver"
        android:enabled="true"
        android:exported="false">
        <intent-filter>
            <action android:name="com.meshkatelhoda.pro.STOP_RAMADAN_REMINDER"/>
        </intent-filter>
    </receiver>


        <!-- MainActivity extends AudioServiceActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Awesome Notifications Receivers - Required for terminated state -->
        <receiver
            android:name="me.carda.awesome_notifications.core.receivers.ScheduledNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="me.carda.awesome_notifications.core.receivers.DismissedNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="me.carda.awesome_notifications.core.receivers.RefreshSchedulesReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name="me.carda.awesome_notifications.core.receivers.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Broadcast receiver for boot completed - reschedule notifications after reboot -->
        <receiver
            android:name="me.carda.awesome_notifications.core.receivers.AwesomeEventsReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>
    
        <!-- أضف الـ service -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- أضف الـ receiver -->
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>